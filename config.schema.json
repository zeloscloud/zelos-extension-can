{
  "type": "object",
  "title": "CAN Extension Configuration",
  "description": "Configure one or more CAN buses to monitor in parallel.",
  "properties": {
    "buses": {
      "type": "array",
      "description": "Configure CAN buses to monitor. Click 'Add' to add a new bus.",
      "title": "Buses",
      "minItems": 1,
      "items": {
        "title": "Bus",
        "type": "object",
        "properties": {
          "interface": {
            "type": "string",
            "title": "Interface",
            "description": "CAN interface type. zelos-socketcan is the Rust-backed SocketCAN interface (Linux only); socketcan is the python-can implementation. macOS/Windows: pcan/kvaser/vector. ssh-socketcan works from any OS, bridging a remote SocketCAN interface over SSH using the edge's own can-utils (candump/cansend); nothing is deployed on the edge and TX is always echoed back by the remote kernel loopback.",
            "enum": [
              "zelos-socketcan",
              "ssh-socketcan",
              "demo",
              "virtual",
              "socketcan",
              "pcan",
              "kvaser",
              "vector",
              "other"
            ]
          }
        },
        "required": [
          "interface"
        ],
        "dependencies": {
          "interface": {
            "oneOf": [
              {
                "properties": {
                  "interface": {
                    "enum": [
                      "demo"
                    ]
                  },
                  "name": {
                    "type": "string",
                    "title": "Name",
                    "description": "Name to identify this bus (used as prefix in trace events)",
                    "ui:placeholder": "can_codec"
                  }
                }
              },
              {
                "properties": {
                  "interface": {
                    "enum": [
                      "virtual"
                    ]
                  },
                  "channel": {
                    "type": "string",
                    "title": "Channel",
                    "description": "Virtual CAN channel identifier (e.g. vcan0)",
                    "default": "vcan0"
                  },
                  "database_file": {
                    "type": "string",
                    "title": "Database File (.dbc)",
                    "description": "Path to CAN database file (supports .dbc, .arxml, .kcd, .sym)",
                    "ui:widget": "file-picker"
                  },
                  "name": {
                    "type": "string",
                    "title": "Name",
                    "description": "Name to identify this bus (used as prefix in trace events)",
                    "ui:placeholder": "can_codec"
                  },
                  "receive_own_messages": {
                    "type": "boolean",
                    "title": "Receive Own Messages",
                    "description": "Receive messages transmitted by this interface",
                    "default": true,
                    "ui:widget": "toggle"
                  },
                  "log_raw_frames": {
                    "type": "boolean",
                    "title": "Log Raw CAN Frames",
                    "description": "Log raw CAN frames (id, dlc, data) to separate 'can_raw' trace source",
                    "default": false,
                    "ui:widget": "toggle"
                  },
                  "emit_schemas_on_init": {
                    "type": "boolean",
                    "title": "Emit Schemas On Init",
                    "description": "Generate and emit all message schemas at startup (true) or lazily as messages are encountered (false)",
                    "default": false,
                    "ui:widget": "toggle"
                  }
                },
                "required": [
                  "channel",
                  "database_file"
                ]
              },
              {
                "properties": {
                  "interface": {
                    "enum": [
                      "socketcan",
                      "zelos-socketcan"
                    ]
                  },
                  "channel": {
                    "type": "string",
                    "title": "Channel",
                    "description": "CAN channel identifier",
                    "default": "can0"
                  },
                  "database_file": {
                    "type": "string",
                    "title": "Database File (.dbc)",
                    "description": "Path to CAN database file (supports .dbc, .arxml, .kcd, .sym)",
                    "ui:widget": "file-picker"
                  },
                  "name": {
                    "type": "string",
                    "title": "Name",
                    "description": "Name to identify this bus (used as prefix in trace events)",
                    "ui:placeholder": "can_codec"
                  },
                  "fd_mode": {
                    "type": "boolean",
                    "title": "CAN-FD Mode",
                    "description": "Enable CAN-FD for higher data rates and larger payloads",
                    "default": false,
                    "ui:widget": "toggle"
                  },
                  "timestamp_mode": {
                    "type": "string",
                    "title": "Timestamp Mode",
                    "description": "Hardware timestamp handling mode",
                    "enum": [
                      "auto",
                      "absolute",
                      "ignore"
                    ],
                    "default": "auto"
                  },
                  "receive_own_messages": {
                    "type": "boolean",
                    "title": "Receive Own Messages",
                    "description": "Receive messages transmitted by this interface (useful for testing and monitoring)",
                    "default": true,
                    "ui:widget": "toggle"
                  },
                  "log_raw_frames": {
                    "type": "boolean",
                    "title": "Log Raw CAN Frames",
                    "description": "Log raw CAN frames (id, dlc, data) to separate 'can_raw' trace source",
                    "default": false,
                    "ui:widget": "toggle"
                  },
                  "config_json": {
                    "type": "string",
                    "title": "Advanced Configuration (JSON)",
                    "description": "Additional python-can Bus() kwargs as JSON object"
                  },
                  "emit_schemas_on_init": {
                    "type": "boolean",
                    "title": "Emit Schemas On Init",
                    "description": "Generate and emit all message schemas at startup (true) or lazily as messages are encountered (false)",
                    "default": false,
                    "ui:widget": "toggle"
                  }
                },
                "required": [
                  "channel",
                  "database_file"
                ],
                "dependencies": {
                  "fd_mode": {
                    "oneOf": [
                      {
                        "properties": {
                          "fd_mode": {
                            "enum": [
                              false
                            ]
                          }
                        }
                      },
                      {
                        "properties": {
                          "fd_mode": {
                            "enum": [
                              true
                            ]
                          },
                          "data_bitrate": {
                            "type": "integer",
                            "title": "Data Bitrate (bps)",
                            "description": "CAN-FD data phase bitrate",
                            "enum": [
                              500000,
                              1000000,
                              2000000,
                              4000000,
                              8000000
                            ],
                            "default": 2000000
                          }
                        },
                        "required": [
                          "data_bitrate"
                        ]
                      }
                    ]
                  }
                }
              },
              {
                "properties": {
                  "interface": {
                    "enum": [
                      "ssh-socketcan"
                    ]
                  },
                  "remote_host": {
                    "type": "string",
                    "title": "Remote Host",
                    "description": "Hostname or IP of the edge device running SocketCAN, reached over SSH (may be a ~/.ssh/config alias)"
                  },
                  "remote_channel": {
                    "type": "string",
                    "title": "Remote CAN Channel",
                    "description": "SocketCAN interface name on the remote edge device (e.g. can0, vcan0)",
                    "default": "can0"
                  },
                  "ssh_user": {
                    "type": "string",
                    "title": "SSH User",
                    "description": "Username for the SSH connection (optional; falls back to your ssh config / current user)"
                  },
                  "ssh_port": {
                    "type": "integer",
                    "title": "SSH Port",
                    "description": "TCP port for the SSH connection",
                    "default": 22
                  },
                  "ssh_key_path": {
                    "type": "string",
                    "title": "SSH Key File",
                    "description": "Path to a private key file for SSH authentication (optional)",
                    "ui:widget": "file-picker"
                  },
                  "ssh_extra_opts": {
                    "type": "string",
                    "title": "SSH Extra Options",
                    "description": "Additional ssh command-line options, space-separated (e.g. -o StrictHostKeyChecking=no)"
                  },
                  "database_file": {
                    "type": "string",
                    "title": "Database File (.dbc)",
                    "description": "Path to CAN database file (supports .dbc, .arxml, .kcd, .sym)",
                    "ui:widget": "file-picker"
                  },
                  "name": {
                    "type": "string",
                    "title": "Name",
                    "description": "Name to identify this bus (used as prefix in trace events)",
                    "ui:placeholder": "can_codec"
                  },
                  "fd_mode": {
                    "type": "boolean",
                    "title": "CAN-FD Mode",
                    "description": "Enable CAN-FD for higher data rates and larger payloads",
                    "default": false,
                    "ui:widget": "toggle"
                  },
                  "timestamp_mode": {
                    "type": "string",
                    "title": "Timestamp Mode",
                    "description": "Timestamp handling for the REMOTE edge clock (not local): 'auto' trusts the remote epoch when it is within ~15s of local time, otherwise maps monotonic remote timestamps onto local wall-clock; 'absolute' uses the remote timestamp as-is; 'ignore' uses the local receive time. A skewed edge NTP/RTC clock will shift trace timestamps.",
                    "enum": [
                      "auto",
                      "absolute",
                      "ignore"
                    ],
                    "default": "auto"
                  },
                  "log_raw_frames": {
                    "type": "boolean",
                    "title": "Log Raw CAN Frames",
                    "description": "Log raw CAN frames (id, dlc, data) to separate 'can_raw' trace source",
                    "default": false,
                    "ui:widget": "toggle"
                  },
                  "emit_schemas_on_init": {
                    "type": "boolean",
                    "title": "Emit Schemas On Init",
                    "description": "Generate and emit all message schemas at startup (true) or lazily as messages are encountered (false)",
                    "default": false,
                    "ui:widget": "toggle"
                  }
                },
                "required": [
                  "interface",
                  "remote_host",
                  "database_file"
                ]
              },
              {
                "properties": {
                  "interface": {
                    "enum": [
                      "pcan"
                    ]
                  },
                  "channel": {
                    "type": "string",
                    "title": "Channel",
                    "description": "CAN channel identifier",
                    "default": "PCAN_USBBUS1"
                  },
                  "database_file": {
                    "type": "string",
                    "title": "Database File (.dbc)",
                    "description": "Path to CAN database file (supports .dbc, .arxml, .kcd, .sym)",
                    "ui:widget": "file-picker"
                  },
                  "bitrate": {
                    "type": "integer",
                    "title": "Bitrate (bps)",
                    "description": "CAN bus bitrate",
                    "enum": [
                      125000,
                      250000,
                      500000,
                      1000000
                    ],
                    "default": 500000
                  },
                  "name": {
                    "type": "string",
                    "title": "Name",
                    "description": "Name to identify this bus (used as prefix in trace events)",
                    "ui:placeholder": "can_codec"
                  },
                  "fd_mode": {
                    "type": "boolean",
                    "title": "CAN-FD Mode",
                    "description": "Enable CAN-FD for higher data rates and larger payloads",
                    "default": false,
                    "ui:widget": "toggle"
                  },
                  "timestamp_mode": {
                    "type": "string",
                    "title": "Timestamp Mode",
                    "description": "Hardware timestamp handling mode",
                    "enum": [
                      "auto",
                      "absolute",
                      "ignore"
                    ],
                    "default": "auto"
                  },
                  "receive_own_messages": {
                    "type": "boolean",
                    "title": "Receive Own Messages",
                    "description": "Receive messages transmitted by this interface (useful for testing and monitoring)",
                    "default": true,
                    "ui:widget": "toggle"
                  },
                  "log_raw_frames": {
                    "type": "boolean",
                    "title": "Log Raw CAN Frames",
                    "description": "Log raw CAN frames (id, dlc, data) to separate 'can_raw' trace source",
                    "default": false,
                    "ui:widget": "toggle"
                  },
                  "config_json": {
                    "type": "string",
                    "title": "Advanced Configuration (JSON)",
                    "description": "Additional python-can Bus() kwargs as JSON object"
                  },
                  "emit_schemas_on_init": {
                    "type": "boolean",
                    "title": "Emit Schemas On Init",
                    "description": "Generate and emit all message schemas at startup (true) or lazily as messages are encountered (false)",
                    "default": false,
                    "ui:widget": "toggle"
                  }
                },
                "required": [
                  "channel",
                  "database_file",
                  "bitrate"
                ],
                "dependencies": {
                  "fd_mode": {
                    "oneOf": [
                      {
                        "properties": {
                          "fd_mode": {
                            "enum": [
                              false
                            ]
                          }
                        }
                      },
                      {
                        "properties": {
                          "fd_mode": {
                            "enum": [
                              true
                            ]
                          },
                          "data_bitrate": {
                            "type": "integer",
                            "title": "Data Bitrate (bps)",
                            "description": "CAN-FD data phase bitrate",
                            "enum": [
                              500000,
                              1000000,
                              2000000,
                              4000000,
                              8000000
                            ],
                            "default": 2000000
                          }
                        },
                        "required": [
                          "data_bitrate"
                        ]
                      }
                    ]
                  }
                }
              },
              {
                "properties": {
                  "interface": {
                    "enum": [
                      "kvaser"
                    ]
                  },
                  "channel": {
                    "type": "string",
                    "title": "Channel",
                    "description": "CAN channel identifier",
                    "default": "0"
                  },
                  "database_file": {
                    "type": "string",
                    "title": "Database File (.dbc)",
                    "description": "Path to CAN database file (supports .dbc, .arxml, .kcd, .sym)",
                    "ui:widget": "file-picker"
                  },
                  "bitrate": {
                    "type": "integer",
                    "title": "Bitrate (bps)",
                    "description": "CAN bus bitrate",
                    "enum": [
                      125000,
                      250000,
                      500000,
                      1000000
                    ],
                    "default": 500000
                  },
                  "name": {
                    "type": "string",
                    "title": "Name",
                    "description": "Name to identify this bus (used as prefix in trace events)",
                    "ui:placeholder": "can_codec"
                  },
                  "fd_mode": {
                    "type": "boolean",
                    "title": "CAN-FD Mode",
                    "description": "Enable CAN-FD for higher data rates and larger payloads",
                    "default": false,
                    "ui:widget": "toggle"
                  },
                  "timestamp_mode": {
                    "type": "string",
                    "title": "Timestamp Mode",
                    "description": "Hardware timestamp handling mode",
                    "enum": [
                      "auto",
                      "absolute",
                      "ignore"
                    ],
                    "default": "auto"
                  },
                  "receive_own_messages": {
                    "type": "boolean",
                    "title": "Receive Own Messages",
                    "description": "Receive messages transmitted by this interface (useful for testing and monitoring)",
                    "default": true,
                    "ui:widget": "toggle"
                  },
                  "log_raw_frames": {
                    "type": "boolean",
                    "title": "Log Raw CAN Frames",
                    "description": "Log raw CAN frames (id, dlc, data) to separate 'can_raw' trace source",
                    "default": false,
                    "ui:widget": "toggle"
                  },
                  "config_json": {
                    "type": "string",
                    "title": "Advanced Configuration (JSON)",
                    "description": "Additional python-can Bus() kwargs as JSON object"
                  },
                  "emit_schemas_on_init": {
                    "type": "boolean",
                    "title": "Emit Schemas On Init",
                    "description": "Generate and emit all message schemas at startup (true) or lazily as messages are encountered (false)",
                    "default": false,
                    "ui:widget": "toggle"
                  }
                },
                "required": [
                  "channel",
                  "database_file",
                  "bitrate"
                ],
                "dependencies": {
                  "fd_mode": {
                    "oneOf": [
                      {
                        "properties": {
                          "fd_mode": {
                            "enum": [
                              false
                            ]
                          }
                        }
                      },
                      {
                        "properties": {
                          "fd_mode": {
                            "enum": [
                              true
                            ]
                          },
                          "data_bitrate": {
                            "type": "integer",
                            "title": "Data Bitrate (bps)",
                            "description": "CAN-FD data phase bitrate",
                            "enum": [
                              500000,
                              1000000,
                              2000000,
                              4000000,
                              8000000
                            ],
                            "default": 2000000
                          }
                        },
                        "required": [
                          "data_bitrate"
                        ]
                      }
                    ]
                  }
                }
              },
              {
                "properties": {
                  "interface": {
                    "enum": [
                      "vector"
                    ]
                  },
                  "channel": {
                    "type": "string",
                    "title": "Channel",
                    "description": "CAN channel identifier",
                    "default": "0"
                  },
                  "database_file": {
                    "type": "string",
                    "title": "Database File (.dbc)",
                    "description": "Path to CAN database file (supports .dbc, .arxml, .kcd, .sym)",
                    "ui:widget": "file-picker"
                  },
                  "bitrate": {
                    "type": "integer",
                    "title": "Bitrate (bps)",
                    "description": "CAN bus bitrate",
                    "enum": [
                      125000,
                      250000,
                      500000,
                      1000000
                    ],
                    "default": 500000
                  },
                  "name": {
                    "type": "string",
                    "title": "Name",
                    "description": "Name to identify this bus (used as prefix in trace events)",
                    "ui:placeholder": "can_codec"
                  },
                  "fd_mode": {
                    "type": "boolean",
                    "title": "CAN-FD Mode",
                    "description": "Enable CAN-FD for higher data rates and larger payloads",
                    "default": false,
                    "ui:widget": "toggle"
                  },
                  "timestamp_mode": {
                    "type": "string",
                    "title": "Timestamp Mode",
                    "description": "Hardware timestamp handling mode",
                    "enum": [
                      "auto",
                      "absolute",
                      "ignore"
                    ],
                    "default": "auto"
                  },
                  "receive_own_messages": {
                    "type": "boolean",
                    "title": "Receive Own Messages",
                    "description": "Receive messages transmitted by this interface (useful for testing and monitoring)",
                    "default": true,
                    "ui:widget": "toggle"
                  },
                  "log_raw_frames": {
                    "type": "boolean",
                    "title": "Log Raw CAN Frames",
                    "description": "Log raw CAN frames (id, dlc, data) to separate 'can_raw' trace source",
                    "default": false,
                    "ui:widget": "toggle"
                  },
                  "config_json": {
                    "type": "string",
                    "title": "Advanced Configuration (JSON)",
                    "description": "Additional python-can Bus() kwargs as JSON object"
                  },
                  "emit_schemas_on_init": {
                    "type": "boolean",
                    "title": "Emit Schemas On Init",
                    "description": "Generate and emit all message schemas at startup (true) or lazily as messages are encountered (false)",
                    "default": false,
                    "ui:widget": "toggle"
                  }
                },
                "required": [
                  "channel",
                  "database_file",
                  "bitrate"
                ],
                "dependencies": {
                  "fd_mode": {
                    "oneOf": [
                      {
                        "properties": {
                          "fd_mode": {
                            "enum": [
                              false
                            ]
                          }
                        }
                      },
                      {
                        "properties": {
                          "fd_mode": {
                            "enum": [
                              true
                            ]
                          },
                          "data_bitrate": {
                            "type": "integer",
                            "title": "Data Bitrate (bps)",
                            "description": "CAN-FD data phase bitrate",
                            "enum": [
                              500000,
                              1000000,
                              2000000,
                              4000000,
                              8000000
                            ],
                            "default": 2000000
                          }
                        },
                        "required": [
                          "data_bitrate"
                        ]
                      }
                    ]
                  }
                }
              },
              {
                "properties": {
                  "interface": {
                    "enum": [
                      "other"
                    ]
                  },
                  "database_file": {
                    "type": "string",
                    "title": "Database File (.dbc)",
                    "description": "Path to CAN database file (supports .dbc, .arxml, .kcd, .sym)",
                    "ui:widget": "file-picker"
                  },
                  "config_json": {
                    "type": "string",
                    "title": "Advanced Configuration (JSON)",
                    "description": "Provide complete python-can Bus() kwargs as JSON. Must include 'interface' and 'channel' keys. Example: {\"interface\": \"slcan\", \"channel\": \"/dev/ttyUSB0\", \"bitrate\": 500000}"
                  },
                  "name": {
                    "type": "string",
                    "title": "Name",
                    "description": "Name to identify this bus (used as prefix in trace events)",
                    "ui:placeholder": "can_codec"
                  },
                  "emit_schemas_on_init": {
                    "type": "boolean",
                    "title": "Emit Schemas On Init",
                    "description": "Generate and emit all message schemas at startup (true) or lazily as messages are encountered (false)",
                    "default": false,
                    "ui:widget": "toggle"
                  }
                },
                "required": [
                  "database_file",
                  "config_json"
                ]
              }
            ]
          }
        }
      },
      "default": []
    },
    "log_level": {
      "type": "string",
      "title": "Log Level",
      "description": "Set the logging verbosity level for all buses",
      "enum": [
        "DEBUG",
        "INFO",
        "WARNING",
        "ERROR",
        "CRITICAL"
      ],
      "default": "INFO"
    }
  },
  "required": [
    "buses"
  ]
}
